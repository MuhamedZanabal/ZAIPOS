/**
 * @deprecated CSS branding is now applied by useTenantByDomain (via TenantProvider)
 * before the user logs in. This file is kept as a placeholder to avoid breaking
 * any stray imports; it is a no-op and will be removed in a follow-up cleanup.
 */
export function useTenantTheme(): void {
  // no-op – intentionally empty
}
