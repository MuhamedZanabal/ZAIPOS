-- ZAIPOS Bahrain hardening.
-- This migration version intentionally creates no seeded authentication users.
-- Demo and production accounts must be provisioned through normal authenticated admin flows.
SELECT 1;
