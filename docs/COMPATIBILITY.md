# Compatibility boundaries

The Bahrain cutover removes foreign-market behavior from active ZAIPOS surfaces. A small number of internal persisted values may remain for data compatibility where destructive renaming would risk historical records or PostgreSQL enum safety.

These compatibility values are not product branding and must not appear as active integrations in the UI, settings, receipts, reports, or documentation.
