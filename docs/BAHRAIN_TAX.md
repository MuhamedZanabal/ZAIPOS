# Bahrain Tax Baseline

ZAIPOS uses a 10% standard VAT default for Bahrain.

The system must still support zero-rated and exempt treatment where configured. Product and tenant tax data must remain explicit; the 10% value is a Bahrain default, not a claim that every transaction is standard-rated.
