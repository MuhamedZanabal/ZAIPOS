# Localization Gate

`node scripts/audit-localization.mjs` is the mandatory repository-wide guard for ZAIPOS product identity, English user-facing text, and Bahrain-native defaults. CI executes it before migrations, lint, tests and build.
