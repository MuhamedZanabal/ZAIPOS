# English UI Policy

ZAIPOS user-facing interface text is English.

Persisted database enum values, migration history, machine identifiers and third-party protocol values are not user-facing language and may remain unchanged when compatibility requires them. Any such value must be mapped to English before rendering.

The localization audit is the enforcement gate for known Spanish UI residue and legacy country/product markers.
