# ZAIPOS Product Identity

Canonical product name: **ZAIPOS**.

Canonical package identifier: `zaipos`.

Canonical repository: `MuhamedZanabal/ZAIPOS`.

Canonical Electron application ID: `com.zaipos.pos`.

All user-facing product names, documentation, installer labels, PWA metadata, deployment examples and generated assets must use ZAIPOS. Compatibility-only persisted identifiers may remain only when changing them would break existing installations or data, and they must not be exposed as the active product identity.
